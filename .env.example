DATABASE_URL="file:./dev.db"

# Optional fallback — normally set via the in-app Setup page instead (stored in the DB).
# Get this from openrouter.ai/keys
OPENROUTER_API_KEY=""

# Optional fallback — normally set via the in-app Setup page instead (stored in the DB).
# Get this from your Ayrshare dashboard after linking Instagram, Facebook Page, and LinkedIn
AYRSHARE_API_KEY=""
